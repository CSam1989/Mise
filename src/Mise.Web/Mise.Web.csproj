<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Mise.ServiceDefaults\Mise.ServiceDefaults.csproj" />
    <ProjectReference Include="..\UI\Mise.UI.Abstractions\Mise.UI.Abstractions.csproj" />
    <ProjectReference Include="..\UI\Mise.UI.Components\Mise.UI.Components.csproj" />
  </ItemGroup>

  <!-- System.IdentityModel.Tokens.Jwt was Phase 2's placeholder token-minting dependency —
       Mise.Web no longer mints tokens at all (StaffIdentity's JwtTokenIssuer does, Phase 3),
       so it's removed rather than left unused. -->

</Project>
