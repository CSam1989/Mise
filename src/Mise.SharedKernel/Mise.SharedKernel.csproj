<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Deliberately zero PackageReference entries: this project must also restore on a
       MAUI Android target framework (Phase 14), so it stays pure BCL. -->

</Project>
